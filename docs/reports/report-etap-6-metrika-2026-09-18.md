# Отчёт: этап 6 — счётчик Метрики [номер счётчика] и отправка форм

Задача: чистая копия moskva.prom-opora.ru. Дата: 2026-09-18.
Диалог: docs/sessions/dialog-sborka-moskva-2026-09-17.md
Скрипты: TMP/50_stage6_metrika.py, TMP/52_stage6_formy.py
Конвейер: 10 → 21 → 32 → 34 → 42 → 43 → 50 → 52 (прогнан целиком, приёмка ЧИСТО).

## 1. Счётчик Яндекс.Метрики

- Стандартный скрипт (вебвизор, clickmap, ecommerce, tracklinks — как в
  CLAUDE.md, id [номер счётчика]) + noscript-заглушка добавлены на все 458 страниц
  перед </head>.
- window.SHOP_YM_ID = [номер счётчика] определён в metrica.js (cart.js берёт оттуда
  номер счётчика для целей корзины; defer-порядок: metrica.js раньше cart.js).

## 2. inc/js/metrica.js (1 837 Б, все страницы)

Собран из сохранённого на этапе 2 promOporaMetrica:
- счётчик [номер счётчика] → [номер счётчика] (3 места);
- удалён слушатель formit:success (движковое событие FormIt, на статике не
  возникает; его роль взял forms.js);
- удалены слушатели ms3:order:* (корзину обслуживает cart.js, цели
  lid_form_cart / lid_form_order он шлёт сам);
- сохранены: обработчик показа модальных окон (data-metrica-modal: 4 цели)
  и снятие onclick с переключателей модалок (защита от двойного счёта);
- добавлены hover-цели (решение владельца): lid_hover_tel (a[href^="tel:"]),
  lid_hover_email (a[href^="mailto:"]), однократно на элемент.

## 3. Отправка форм (решение владельца: формы работают)

Находка этапа 6: формы обратной связи (звонок, вопрос, прайс, подписка,
антидрон) никуда не отправлялись — action пуст, FormIt удалён на этапе 2.

- **site/form.php** (7 986 Б) — обработчик по схеме order.php: вход JSON,
  honeypot (скрытое поле website), whitelist formid (subscribe, callback,
  question, reward, pricelist, antidrone), валидация (подписка — только
  e-mail; остальные — имя и телефон или почта, согласие), журнал
  zayavki.csv (CP1251, «;»), письмо через SMTP smtp.timeweb.ru:587 STARTTLS
  на [почта цели moskva] + скрытая копия [адрес скрытой копии], без внешних
  библиотек.
- **inc/js/forms.js** (2 659 Б, все страницы) — перехват отправки форм с
  data-metrica-goal: сбор полей, запрос на /form.php; при успехе — сообщение
  в data-shopform-success-message, цель data-metrica-goal (lid_form_subscribe,
  lid_form_send_callback, lid_form_send_question, lid_form_send_reward,
  lid_form_send_price, lid_form_antidrone), сброс формы, закрытие модального
  окна через 2,5 с; при ошибке — data-shopform-validation-error-message,
  кнопка разблокируется. Кнопка блокируется на время отправки.
- Формы корзины (data-shop-form="order") не затронуты — их ведёт cart.js.

## Приёмка

- На каждой странице ровно один счётчик и один noscript [номер счётчика]; остатков
  [номер счётчика] в html/js/css/php нет; в metrica.js нет formit/ms3; forms.js и
  metrica.js подключены на всех страницах. Журналы: inventory/etap-6/.
- Браузерный тест (Playwright, страницы главная/корзина/карточка):
  SHOP_YM_ID = [номер счётчика], honeypot добавляется в формы, скрипт счётчика в
  DOM, promOporaMetrica загружен, JS-ошибок нет.
- Тест отправки подписки при недоступном обработчике (локально нет PHP):
  перехват срабатывает, показывается сообщение об ошибке, JS не падает.
  Успешный путь (письмо, zayavki.csv) проверяется на этапе 7 на хостинге
  после деплоя.

## Ограничения и на что обратить внимание

1. Синтаксис form.php проверен структурно (баланс скобок, теги); PHP-CLI
   на локальной машине нет — полная проверка выполнятся на этапе 7 в браузере
   на хостинге (демо-заявка + письмо).
2. Скрипт Callibri (cdn.callibri.ru) остался в разметке — внешний сервис
   коллтрекинга, работает и на живом сайте; отключение — решение владельца.
3. Цели «клик по телефону/email/мессенджерам» живут в onclick-атрибутах
   (переведены на [номер счётчика] ещё на этапе 2); hover-цели — в metrica.js.
   Полный перечень целей для кабинета Метрики — в отчёте этапа 6 (этот файл,
   разделы 2–3) и в CLAUDE.md.

Дополнение 2026-09-19: перечень собран заново из разметки и скриптов сборки
(forms.js, metrica.js, cart.js, onclick) и оформлен отдельным файлом-перечнем
для заведения целей в кабинете:
- inventory/etap-6/celi-metrika-moskva.csv (22 цели, тип «JavaScript-событие»);
- inventory/etap-6/celi-metrika-moskva-2026-09-19.md (то же, markdown-таблицы).
В перечне выделены легаси-цели gethelp и reward (7 и 8 мест на странице
izgotovlenie-metallokonstrukczij-na-zakaz.html) — их в Метрику НЕ вносить:
это дубль открытия модалки #reward, уже учтённый целью open_modal_form_reward.
Статус 2026-09-19: цели добавлены владельцем в кабинет Метрики; статистика
считается с даты заведения.

## Файлы

- Созданы: TMP/50_stage6_metrika.py, TMP/52_stage6_formy.py,
  site/inc/js/metrica.js, site/inc/js/forms.js, site/form.php,
  inventory/etap-6/zhurnal-metrika.csv, inventory/etap-6/zhurnal-formy.csv.
- Изменены: все 458 html (счётчик, noscript, два тега script).

Этап 6 завершён. Далее этап 7: финальная приёмка, поведенческий тест
корзины/фильтра/поиска/заказа/форм, robots/sitemap, деплой после подтверждения.