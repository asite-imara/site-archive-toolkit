# Отчёт. Этап 7: приёмка сборки moskva.prom-opora.ru

Дата: 2026-09-18.
Сборка: `sborka-moskva-prom-opora-2026-09-17/site/` (458 страниц).
Статус этапа: пункты 1–2 выполнены (robots/sitemap, статическая приёмка, скриншоты).
Пункт 3 (выкладка на хостинг) — ожидает подтверждения владельца.

## 1. robots.txt и sitemap.xml

- `robots.txt`: строка Sitemap заменена на `https://moskva.prom-opora.ru/sitemap.xml`.
  Остальные правила без изменений.
- `sitemap.xml`: домен заменён во всех `<loc>` (459), исключена запись
  `/service/cart-count/` (заглушка 11 байт, в сборку не включена). Итог — 458 адресов,
  сверены с фактическими страницами сборки: 458/458. XML валиден.
- Особенность (наследие живого сайта, оставлено как есть): `/service/` запрещён в
  robots, но присутствует в sitemap — то же состояние, что на prom-opora.ru.

## 2. Статическая приёмка

Инструмент: `TMP/etap7_priemka.py` (новый скрипт этапа). Проверено 98 994 ссылки и
ресурса: 458 html, 9 css, 11 js. Журнал: `inventory/etap-7/priemka-final.txt`.

### Исправлено (журналы в `inventory/etap-7/`)

| Что | Объём | Журнал |
|---|---|---|
| Битые `@font-face` на движковые шрифты (open-sans, Woo-Side-Cart) в 458 страницах — блоки удалены | 2290 блоков | zhurnal-pravok-priemka.csv |
| Ссылки yootheme-фонов в инлайн-стилях 458 страниц — url заменён на none | 912 | zhurnal-pravok-priemka.csv |
| Ссылки на стейджинг-домен `new.prom-opora.ru` — переведены в относительные | 22 | zhurnal-pravok-priemka.csv |
| Битые `@font-face` в common.css и custom.css | 10 блоков | zhurnal-css.csv |
| yootheme-url в common.css и custom.css | 12 | zhurnal-css.csv |
| uikit-url в common.css и theme-child.css | 4 | zhurnal-css-uikit.csv |
| Битые кандидаты srcset без расширения (страница контактов ЖД-сетей) | 7 | zhurnal-srcset.csv |

Все правки применялись скриптом по всем страницам; на живом сайте эти ссылки
тоже вели на 404 (унаследованный мусор движка).

### Восстановлено из архива (site-backup-html)

Этап 1 не включил в сборку дерево `assets/` — карточки товаров ссылались на
`/assets/images/products/...`. Восстановлено 4221 файл (только реально
используемые, суммарно около 184 МБ):

- 4026 файлов в `site/assets/images/products/` (галереи карточек);
- 191 файл ранее (первый прогон, подмножество тех же деревьев);
- 4 файла `site/upload/lp/opora-kont-seti-*` (страница контактов ЖД-сетей).

Списки: `TMP/etap7-nujnye-assets2.txt`, `TMP/etap7-nujnye-assets3.txt`.

### Итог приёмки: 159 оставшихся ссылок на отсутствующее — оба унаследованы от живого сайта

1. `/upload/video-svai.mp4` — 121 страница винтовых свай. Файла нет ни в архиве,
   ни на живом сайте (HTTP 404 на prom-opora.ru). Оставлено как есть; если видео
   появится — достаточно положить один файл в `/upload/`.
2. Битые `src` картинок на «О компании» (38) — перенос строки внутри атрибута
   в исходной разметке живого сайта. Относится к блоку Г: решение владельца было —
   заглушки в видимые слоты «О компании» не ставить, слоты проверить владельцу.

### Прочие проверки

- Абсолютные ссылки на свои домены: 0.
- Остатки движка (wp-content, ms3_, formit, woocommerce): 0.
- Старый счётчик Метрики [номер счётчика]: 0.
- Шрифты: ровно 3 ссылки на Montserrat в CSS, все 3 файла в `inc/fonts/` —
  прежнее замечание «2 из 5 отсутствуют» снято (лишние начертания отвалились
  при чистке CSS на этапе 5).
- В common.css/custom.css осталось имя шрифта `Woo-Side-Cart` в `font-family` —
  это не ссылка на файл, загрузки нет, отрисовка как на живом сайте.

## 3. Скриншоты ключевых страниц

`inventory/etap-7/skriny/` — 22 снимка (11 страниц × 2 вьюпорта 1366×900 и
375×812), скрипт `TMP/53_etap7_skrinshoty.py`, детерминированный протокол этапа 5.
Проверено визуально: главная, карточка товара, корзина (пустое состояние),
поиск «опора» — рендер корректный, цены из JSON, галереи работают.

## Решения и открытые вопросы к владельцу

1. Выкладка: жду подтверждения для FTP-деплоя в
   `/home/p/promopora/moskva/public_html`.
2. Отображаемые контакты: в подвале и шапке `[почта в контактах]` и
   `+7 (XXX) XXX-XX-XX` — как на живом сайте. Для московской версии, возможно,
   нужен `[почта цели moskva]` — массовая правка по 458 страницам, требует
   решения (это же касается городов/адресов в тексте, если они есть).
3. Видео винтовых свай отсутствует (и на живом сайте) — положить файл или
   убрать блоки: на усмотрение владельца.
4. Поведенческий тест (корзина, фильтр, поиск, формы, письмо SMTP, цели
   Метрики) — после выкладки на хостинг: PHP-CLI локально нет.

## 4. Выкладка на хостинг (2026-09-18, подтверждена владельцем)

- Целевая папка `/home/p/promopora/moskva/public_html` до выкладки была пуста
  (только заглушка `.indexhtm` и системный `cgi-bin`).
- Загружено 5096 файлов в 1138 каталогов (около 300 МБ) за 9 минут, 8
  параллельных FTP-соединений, без ошибок. Журнал:
  `inventory/etap-7/zhurnal-deploy.csv`. Скрипт: `TMP/54_etap7_deploy.py`.

### Доработка адресов перед выкладкой

Обнаружено расхождение: canonical страниц и внутренние ссылки — на `.html`,
а sitemap (наследие живого сайта) — на адреса со слешем, которые без правил
сервера дали бы 404. Приведено к единому виду:

- `.htaccess`: добавлены 301-редиректы со слеш- и безрасширенийных адресов на
  `.html` (с явным `https://` — за прокси Apache считает запрос http),
  `DirectoryIndex index.html`, `ErrorDocument 404 /404.html`.
- `sitemap.xml`: 456 адресов переведены на `.html`, исключена служебная
  страница `/404/`; осталось 457 адресов, XML валиден.

### Поведенческий тест на хостинге

- HTTP: главная, robots.txt, sitemap.xml, prices.json, страницы, восстановленные
  ресурсы — 200; `/katalog/opora-ogkf-4/` и `/katalog/` — 301 на https `.html`.
- `form.php` (форма заявки): тестовый POST — `ok:true`, номер
  20260918-091243-1963, запись в `zayavki.csv` (CP1251) на месте,
  `mail-errors.log` не создан — письмо SMTP отправлено ([почта цели moskva],
  копия [адрес скрытой копии]).
- `order.php` (заказ из корзины): тестовый POST — `ok:true`, номер
  20260918-091342-2022, запись в `orders.csv` корректна (товары, сумма, город),
  ошибок почты нет.
- На оба письма приходят тестовые заявки с пометкой «TEST deploy» —
  отвечать на них не нужно.
- Поиск, корзина, фильтр, цели Метрики — клиентские скрипты, проверены ранее
  локально (скриншоты этапа 7); на хостинге отличаются только серверные
  обработчики, они протестированы выше. Контрольный прогон в браузере на
  живом адресе остаётся за владельцем.